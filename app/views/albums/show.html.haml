.container
  .row
    %p 注文番号: #{@album.order_id}
    - @album.photographs.each do |photo|
      = link_to image_tag(photo.url) , photo.url
= link_to "編集", edit_album_path(@album)
= link_to "削除", @album ,method: :delete, confirm: 'Are you sure?'
= link_to "戻る", albums_path
